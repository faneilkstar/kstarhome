╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║      📋 COMMANDES EXACTES - COPIER-COLLER DANS PYCHARM       ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝


⚠️ IMPORTANT: OUVREZ LE TERMINAL PYCHARM (EN BAS) ET SUIVEZ CES ÉTAPES !


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ÉTAPE 1: ACTIVER LE VENV (OBLIGATOIRE !)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copiez-collez cette commande dans le Terminal PyCharm:

   source venv/bin/activate

Vous DEVEZ voir (venv) apparaître au début de la ligne !


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ÉTAPE 2: NETTOYER LES ANCIENS FICHIERS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copiez-collez ces 3 commandes:

   rm -rf migrations
   rm -f instance/*.db
   rm -f site.db app.db


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ÉTAPE 3: CONFIGURER .ENV AVEC VOTRE URL SUPABASE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A) Créer .env:

   cp .env.example .env

B) Éditer .env:

   nano .env

C) Ajouter cette ligne (REMPLACEZ le mot de passe !):

   SUPABASE_DB_URL=postgresql://postgres.pzzfqduntcmklrakhggy:VotreMotDePasse@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

D) Sauvegarder: Ctrl+O, Enter, Ctrl+X


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ÉTAPE 4: INITIALISER FLASK-MIGRATE (3 COMMANDES)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copiez-collez ces 4 commandes UNE PAR UNE:

1. Définir FLASK_APP:

   export FLASK_APP=run.py

2. Initialiser le système:

   flask db init

3. Générer la migration:

   flask db migrate -m "Creation tables Supabase"

4. Appliquer sur Supabase:

   flask db upgrade


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ÉTAPE 5: CRÉER LE COMPTE ADMIN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copiez-collez:

   python create_admin.py

Puis suivez les instructions ou appuyez sur Enter pour:
   - Identifiant: admin
   - Mot de passe: admin123


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ÉTAPE 6: LANCER L'APPLICATION 🚀
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copiez-collez:

   python run.py

Puis ouvrez dans le navigateur:

   http://localhost:5000

Connectez-vous avec:
   👤 admin
   🔑 admin123


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ RÉSUMÉ COMPLET (TOUTES LES COMMANDES)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Pour copier-coller d'un coup (après avoir configuré .env):

# 1. Activer venv
source venv/bin/activate

# 2. Nettoyer
rm -rf migrations
rm -f instance/*.db
rm -f site.db app.db

# 3. Créer .env (puis éditer)
cp .env.example .env
nano .env

# 4. Initialiser Flask-Migrate
export FLASK_APP=run.py
flask db init
flask db migrate -m "Creation tables Supabase"
flask db upgrade

# 5. Créer admin
python create_admin.py

# 6. Lancer
python run.py


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 ALTERNATIVE: SCRIPT AUTOMATIQUE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Si vous préférez, utilisez le script qui fait TOUT:

   source venv/bin/activate
   ./COMMANDES_SETUP.sh

OU

   source venv/bin/activate
   ./setup_supabase.sh


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

• GUIDE_PYCHARM_SUPABASE.txt ... Guide détaillé
• 🚀_COMMENCER_ICI_SUPABASE.txt ... Guide express
• CORRECTIFS_APPLIQUES.txt ....... Problèmes corrigés


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
KSTAR-HOME V3.0 - Production Ready avec Supabase
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

